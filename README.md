# dotfiles
설정파일 관리


## 명령어
```bash
git clone https://github.com/SonByeongJin/dotfiles ~/dotfiles
ln -s ~/dotfiles/[file_name] [file_path]/[file_name]
# ln -s ~/dotfiles/CLAUDE.md ~/.claude/CLAUDE.md
```

```bash
# 디렉토리가 없는 경우
mkdir -p [file_path]
# mkdir -p ~/.claude
```
